
/**
 * First we will load all of this project's JavaScript dependencies, like
 * Axios and anything else you want globally available.
 */

require('./bootstrap')

/**
 * Next, we'll load in the base index of the Typescript/React app.
 */

require('./root.tsx')
